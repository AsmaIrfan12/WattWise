fastapi==0.115.6
uvicorn[standard]==0.32.1
pydantic==2.10.3
pydantic-settings==2.7.0
email-validator==2.2.0

# Database
sqlalchemy==2.0.40
aiomysql==0.2.0
alembic==1.14.0

# Auth
python-jose[cryptography]==3.3.0
bcrypt==4.2.1
passlib[bcrypt]==1.7.4

# MQTT
paho-mqtt==2.1.0

# InfluxDB
influxdb==5.3.2

# HTTP client (for Expo push & HA)
httpx==0.28.1

# Scheduler
apscheduler==3.10.4

# IANA timezone DB — python:3.12-slim ships no system tz data, so
# zoneinfo("Europe/London") would fail without this. Required for
# correct UK peak-tariff windows across GMT/BST.
tzdata==2024.1

# Persona segmentation — unsupervised clustering + cluster-quality metrics
numpy==2.1.3
scikit-learn==1.5.2

# Utilities
python-multipart==0.0.20
python-dotenv==1.0.1

# Observability — structured JSON logging (set LOG_FORMAT=json in production)
python-json-logger==2.0.7

# Testing
pytest==8.3.4
pytest-asyncio==0.24.0
