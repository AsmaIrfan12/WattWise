# Force LF on files that run on Linux RPis / in containers, so they stay valid
# even when checked out or copied from Windows.
*.sh            text eol=lf
run.sh          text eol=lf
Dockerfile      text eol=lf
*.service       text eol=lf
*.yaml          text eol=lf
*.yml           text eol=lf
